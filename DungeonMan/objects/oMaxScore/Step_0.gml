/// @description Insert description here
// You can write your code in this editor


//global.maxScore = global.scores;

if(room == rm_esc_endGame)
{
	if(global.maxScore < global.scores)
		global.maxScore = global.scores;
}

