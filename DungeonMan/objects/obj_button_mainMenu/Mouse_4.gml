/// @description Insert description here
// You can write your code in this editor









// Inherit the parent event event_inherited();

audio_play_sound(clicking, 10, false);
global.scores = 0;
with(oMaxScore)
{
	draw_text(0,0,string(" "));
}
room_goto(rm_menu);

